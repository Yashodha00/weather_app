name: weather_app
description: A weather application using OpenWeatherMap API
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  provider: ^6.0.5
  geolocator: ^10.0.0
  shared_preferences: ^2.2.2
  intl: ^0.18.1
  lottie: ^2.7.0
  syncfusion_flutter_gauges: ^23.2.6
  syncfusion_flutter_charts: ^23.2.6
  connectivity_plus: ^5.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true